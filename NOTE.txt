Based on that use of "npm install" will lose some important file of library. So after using the command("npm install,") that also need to use this command to install the missing file of library: "npm install antd --save"